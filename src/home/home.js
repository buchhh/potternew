// var test = require('../../config'); 

app.controller('homeController', function ($scope, $http, $location, $route, $routeParams, query, modal, $window, $document) {
    $scope.user;
    $scope.pwd;
    $scope.messageError;
    $scope.statusLogin = '';
    $scope.statusL == 'unset';
    
});